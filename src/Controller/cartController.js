

export default {
    createCart: async (req, res) => {
        try {
            const {cart} = req.body
            const cartArr = JSON.parse(cart);
            
        } catch (error) {
            
        }
    }
}